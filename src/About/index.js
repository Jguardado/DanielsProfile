import React from 'react';
import style from './index.css';

const About = ({}) => {

    return (
        <div className={style}>
            <div>ABOUT</div>
        </div>
    );
};

About.propTypes = {

};

export default About;
