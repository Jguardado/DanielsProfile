import React from 'react';
import style from './index.css';

const Projects = ({}) => {

    return (
        <div className={style}>
            <div>PROJECTS</div>
        </div>
    );
};

Projects.propTypes = {

};

export default Projects;
